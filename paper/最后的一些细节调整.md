“hardware platform” (Mao 等, p. 1) 和后面实验部分的 Platform Configuration 重复，我认为这里可以直接引用后面的对应内容，而不用自己再写一遍。

“Hot access completes in approximately 30 ns, while cold access requires approximately 3 μs. This represents a roughly 100× latency gap.” (Mao 等, p. 1) 冷访问和热访问时间的具体数据呢？我们应该是在一个 dev 网络上做了实验测试的，怎么这里没有写？

“Cold accesses account for 76% of all storage slot access events3.” (Mao 等, p. 1) 这里是否解释清楚了比例的内涵呢？是没有加权计算的情况，而不是真实的流量情况。

“A single inference takes approximately 28 ms, while a single prefetch saves at most 3 μs.” (Mao 等, p. 2) 推理开销的数据来源是我们自己的实验，是否需要标注说明一下？

“It represents the fraction of cold accesses that the ML model can successfully predict.” (Mao 等, p. 2) 这里 alpha 参数的定义，待确定

“raw overhead” (Mao 等, p. 2) 这里 raw overhead 指的是什么？没说清楚。应该是指推理开销。

“1,001 consecutive Ethereum mainnet blocks” (Mao 等, p. 2) 区块数量是 1001 个，检查一下全文对总区块数量的表述是否统一。

“From a new perspective,” (Mao 等, p. 4) 这里只有 From a new perspective，但后面两句都有 from a new architecture perspective 和 from a new scene perspective，感觉第一句就怪怪的。

“A pattern key is a 2tuple (to, selector), where to is the target contract address and selector is the 4-byte function selector of the called function.” (Mao 等, p. 5) 前面 preliminary 中已经有了对 pattern key 的解释，这里再解释是否重复了？需要删去吗？

“Over 80% of pattern keys have union sizes below ten.” (Mao 等, p. 5) 和图片内容不相符！我也不清楚这个 80%的数据是从哪来的，可能是模型的幻觉的吧？

“debug_traceTransaction” (Mao 等, p. 5) 这个带下划线的名字应该保留，这是以太坊官方使用的 RPC 接口名，不应该修改。  
  
我觉得是这样的。

“Two outputs are produced from the training phase: the online hash table and a trained multi-label classification model. The input features are selected based on two criteria” (Mao 等, p. 5) 这里直接过渡到 Input features 的选择，似乎有点生硬？感觉中间可以加一点过渡，比如 classification model 需要一些 feature 来完成分类。

“Over 80% of pattern keys have union sizes below ten” (Mao 等, p. 6) Fig4，这里的数据是有问题的，和图片不相符，必须修改！

“a training window” (Mao 等, p. 6) 叫 training window 还是 incremental window，可以斟酌一下。

“Algorithm 1.” (Mao 等, p. 6) 确定算法内容和格式上的正确性，格式似乎存在一些问题，比如左侧的行号全部都是 0？以及最后的 return C = 0 是什么意思？

“pattern generation” (Mao 等, p. 6) 这个 pattern generation 是弃用的旧名字，现在统一改成 candidate accumulation，这里需要修改。

“Sactual (t)” (Mao 等, p. 7) 这里的符号和前面 preliminary 那一节有点不统一：之前用 tx 来表示交易，现在用 t 来表示交易，或许需要统一一下？

“(pattern key, storage slot)” (Mao 等, p. 7)

“(pattern key, slot)” (Mao 等, p. 7) 这里和前面表述不统一的，前面是 (pattern key, storage slot)，这里就只有 (pattern key, slot)。可能需要统一。

“999 blocks.” (Mao 等, p. 7) 前面说总共 1001 个区块，为什么过滤之后就只剩 999 个区块了，为什么少了两个？是不是需要说清楚？
